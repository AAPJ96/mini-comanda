<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/container_background"
    android:orientation="vertical"
    android:padding="16dp"
    tools:context=".ui.historial.HistorialFragment">

    <!-- Botones de alternancia -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:layout_marginBottom="16dp"
        android:baselineAligned="false"
        android:gravity="center_vertical">

        <com.google.android.material.button.MaterialButton
            android:id="@+id/btnResumen"
            style="@style/Widget.Material3.Button"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:layout_marginEnd="8dp"
            android:layout_gravity="center_vertical"
            android:text="Resumen"
            android:textColor="@android:color/white"
            android:backgroundTint="@color/blue"
            app:cornerRadius="8dp" />

        <com.google.android.material.button.MaterialButton
            android:id="@+id/btnComandasPagadas"
            style="@style/Widget.Material3.Button"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:layout_gravity="center_vertical"
            android:text="Comandas pagadas"
            android:textColor="@android:color/white"
            android:backgroundTint="@color/blue"
            app:cornerRadius="8dp" />
    </LinearLayout>

    <!-- Contenedor de las dos vistas -->
    <FrameLayout
        android:id="@+id/containerHistorial"
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <!-- Vista Resumen -->
        <ScrollView
            android:id="@+id/layoutResumen"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:visibility="visible">

            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="vertical">

                <!-- Dropdown de agrupación -->
                <com.google.android.material.textfield.TextInputLayout
                    android:id="@+id/tilAgrupacion"
                    style="@style/Widget.MaterialComponents.TextInputLayout.OutlinedBox.ExposedDropdownMenu"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:hint="Agrupar por"
                    app:boxBackgroundColor="@android:color/white"
                    app:boxStrokeColor="@color/blue">

                    <AutoCompleteTextView
                        android:id="@+id/spinnerAgrupacion"
                        android:layout_width="match_parent"
                        android:layout_height="wrap_content"
                        android:inputType="none"
                        android:text="Diario"
                        android:popupBackground="@android:color/white" />
                </com.google.android.material.textfield.TextInputLayout>

                <!-- Tarjeta de resultados -->
                <com.google.android.material.card.MaterialCardView
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:layout_marginTop="12dp"
                    app:cardCornerRadius="12dp"
                    app:cardBackgroundColor="@android:color/white"
                    app:cardElevation="2dp">

                    <LinearLayout
                        android:id="@+id/resultadosContainer"
                        android:layout_width="match_parent"
                        android:layout_height="wrap_content"
                        android:orientation="vertical"
                        android:padding="16dp">

                        <TextView
                            android:layout_width="match_parent"
                            android:layout_height="wrap_content"
                            android:text="Resultados"
                            android:textAppearance="?attr/textAppearanceHeadline6"
                            android:textColor="@color/blue" />

                        <TextView
                            android:id="@+id/tvContenidoResumen"
                            android:layout_width="match_parent"
                            android:layout_height="wrap_content"
                            android:layout_marginTop="8dp"
                            android:text="Aquí se mostrará el resumen (totales, cantidad de comandas, platillos más pedidos...)"
                            android:textAppearance="?attr/textAppearanceBody1" />

                        <!-- Espacio para futuro contenido dinámico -->
                    </LinearLayout>
                </com.google.android.material.card.MaterialCardView>
            </LinearLayout>
        </ScrollView>

        <!-- Vista Comandas Pagadas (RecyclerView) -->
        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/recyclerPagadas"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:visibility="gone"
            tools:listitem="@layout/item_comanda" />
    </FrameLayout>

</LinearLayout>